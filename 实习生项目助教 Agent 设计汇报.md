## 一、整体定位

我们设计的是一个“实习生项目助教 Agent”。

它不是替代实习生完成项目，而是帮助实习生做每日和阶段性任务辅助：每天和实习生交互，收集日报，确认任务进展，规划次日工作，并根据项目状态调整中长期任务规划，保证实习生按正确路径推进。

---

## 1. 目标和停止条件（团队）

### 目标

每日、阶段性任务辅助，每天和他交互，上传日报/对于任务进行确认跟进，进行次日工作规划，并进行整体的中长期任务规划调整，保证实习生按正确路径推进。

### 停止条件

- 工作时间结束
- 关键任务完成
- 导师确认项目结束

---

## 5. 验证变好（杜宇）

为了验证这个 Agent 是否真的有效，可以设计成每日循环 + 阶段循环。

### 每日 Loop

```text
读取项目状态 → 询问今日进展 → 对比计划 → 识别阻塞 → 给下一步建议 → 更新风险分
````

### 阶段 Loop

```
检查阶段产物 → 找缺口 → 判断是否需要导师介入
```

### Eval 指标（可实现性，量化性、自动化）

- 目标清晰度：是否能说清项目要解决什么问题
- 进度可信度：日报是否有证据支撑（误差）
- 阻塞暴露率：是否及时说出卡点
- 产物质量：代码、数据、报告是否达标
- 风险识别准确率：Agent 提醒的问题导师是否认可

---

## 2. Context（钟俊）

Agent 需要维护多层 Context，保证它既能理解当前任务，也能结合历史信息做判断。

### Context 组成（需要结构化）

- System Prompt —— [[角色]]定位、红线、语气规范、升级规则摘要
- Tools —— 工具名称 + 关键参数表（task_board.update_status 等）
- Skill 元数据 —— skill_id、trigger_condition、applicable_project_type、version 等字段
- 项目状态 —— 原来的里程碑/风险清单，归到这一层（跟随项目周期，不是长期记忆）
- 长期记忆 —— 实习生画像、历史案例、导师偏好
- 短期记忆 —— 按你说的拆成三块：用户初始诉求（逐字保留）、短期压缩记忆（滚动摘要）、短期工具结果（用完即丢）

---

## 3. Tools / RAG 知识库（徐国洪）

### 工具调用集

- 文档解析工具：读取实习生上传代码/报告/数据集，自动校验格式、逻辑；
- 进度规划工具：拆分项目里程碑、生成每日可执行任务清单；
- 风险检测工具：比对产出物与标准模板，自动识别低质量内容；
- 消息通知工具：定时推送进度提醒、风险告警至实习生/导师；
- 评审辅助工具：生成标准化自查清单、修改意见模板。
- 学习路线DAG生成工具：生成学习内容的拓扑排序，规划合理学习路线

### RAG 知识库

- 项目通用规范库：各类小项目（数据分析/工具开发/报告）交付标准、模板；
- 实习流程库：实习生工作规范、导师介入判定规则；
- 问题解决方案库：历史同类实习生踩坑案例、标准答疑话术。
- 助教教研原则文档：
    1. 禁止直接替代实习生执行任务
    2. 注意师德规范
- 内部学习资料，外部学习资料

---

## 6. Skill(团队)

这个 Agent 可以沉淀出几类关键 Skill：

- 项目拆解 Skill：把模糊项目拆成阶段、任务、验收标准
- 风险识别 Skill：识别延期、目标漂移、假进展、质量不稳
- 阶段验收 Skill：按项目类型检查代码、报告、数据结果
- 写报告的 Skill：写日报周报
- 导师介入 Skill：判断什么时候需要导师介入

---

## 4. Harness（兰凯崴）

Harness 主要负责控制 Agent 的边界，保证整个系统安全、可控、可审计。

（怎么做）
### 关键控制点

- 触发规则 —— 日报提交、里程碑到期、连续两次无进展、风险分超过阈值
- 输出约束 —— 每条风险必须包含 `risk_type、evidence、severity、suggested_action`
- 权限与隐私 —— 默认只读、项目数据隔离、禁止访问其他实习生内容
- 异常处理 —— 证据不足或工具失败时标记 `NEED_REVIEW`，停止执行并转人工
- 审计日志 —— 保存工具调用、风险判断、审批人和通知记录

### 整体原则

整体原则是最小权限、高风险操作人工确认、判断必须有证据。

---
## 最终效果：

让实习生的项目过程更加可跟踪，让导师更早发现风险，也让项目验收更有依据。  